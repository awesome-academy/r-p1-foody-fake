<li>
  <%= gravatar_for user, size: Settings.user_size %>
  <%= link_to user.name, user %>
  <% if current_user.check_type("admin") && !current_user?(user) %>
    | <%= link_to t("delete"), deleteuser_path(id: user.id), method: :delete,
      data: { confirm: t("you_sure") } %>
  <% end %>
</li>
