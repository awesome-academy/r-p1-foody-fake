<% provide :title, t("all_users") %>
<div class="container area-info">
  <h1 class="green-b"><%= t("all_users") %></h1>
  <%= paginate @users, theme: "twitter-bootstrap-3"%>
  <ul class="users">
    <%= render @users %>
  </ul>
  <%= paginate @users, theme: "twitter-bootstrap-3" %>
</div>
