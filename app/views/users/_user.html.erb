<li>
  <%= gravatar_for user, size: Settings.user_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("delete"), admin_delete_path(@user)
      data: { confirm: t("you_sure") } %>
  <% end %>
</li>
